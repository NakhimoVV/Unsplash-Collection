Architecture source of truth:
- Follow `docs/architecture-rules.md`;
- When changing the project, update `docs/architecture.md` and use it while working.

## Rules
- Follow existing project structure
- Use TypeScript strictly
- Do not use `any`
- Do not introduce new dependencies without need
- Ask questions whenever there is ambiguity
- avoid magic numbers
- use project package manager - "pnpm"
- Answer in Russian